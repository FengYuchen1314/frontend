# Bug 修复的配套镜像与浏览器检查

日期：2026-09-06。这里只记录指定版本的实测，不代表所有原始需求或 HeroUI 迁移已经完成。

## 本次实际部署的版本

- Backend：`c5b9d397a7df46e766c61aa39c62e672e56b96cc`
- Frontend：`eb8f1790ec080787091e4c00cdaf05cdaf7b04e4`
- [精确配套镜像 Actions](https://github.com/FengYuchen1314/backend/actions/runs/33976781168)：成功。
- [Frontend Actions](https://github.com/FengYuchen1314/frontend/actions/runs/33976780218)：126 项测试、类型检查、全仓 lint 和编译通过。
- 镜像：`ghcr.io/fengyuchen1314/backend@sha256:e7d3a434cd0f81a590d9e2aa7811dfbd9d4f5c5e2b928c0ce3250bebbb247e61`

应用由 GitHub Actions 编译。升级前用无网络的临时容器核对镜像内前后端 SHA 和 dev 配置，然后只替换专用隔离面板容器。没有在本机或 VPS 编译应用。

## 数据保留及环境

升级前保存私有完整配置快照及 PostgreSQL 备份，并保留旧停止容器供恢复。升级后深度比较通过：2 个编排、6 个完整配置、4 个 Host 和订阅设置完全一致。其他运行容器 ID 未变，PDF HTTP 状态仍为 200，没有新增公网端口或主机特权。

第一次检查遇到旧测试 token 过期，受保护 API 返回 401，检查在替换旧容器前停止。改用原测试账号正常登录取得会话后重试成功；没有创建新的持久化凭据、修改密码或绕过认证。

私有快照、数据库备份、会话和恢复脚本留在测试专用目录，不随仓库上传。保留旧容器不等于对任意数据库迁移提供自动回滚保证。

## 真实浏览器检查

通过本机回环 SSH 隧道连接隔离面板，使用生产构建、真实 React 挂载和面板 API：

| 检查                    | 结果与范围                                                                                                                                                             |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 响应头连续编辑/保存     | 将测试环境的更新间隔从 12 改为 13，连续输入后点击保存，完整重新加载后读回 13；随后保存回 12。两次动作总往返约 335ms，不声称测量了浏览器内部事件间隔小于旧 100ms 延迟。 |
| 自定义备注连续编辑/保存 | 修改一条测试备注，连续输入后保存，完整重新加载后读回新值，再恢复原值。两次动作总往返约 310ms，不替代对旧延迟逻辑的单元回归。                                           |
| 平铺入站列表高度        | 真实抽屉中视口为 348px，内容高度为 420px，列表不再为 0 高度。                                                                                                          |
| 平铺入站滚动            | 用滚轮滚到尾部，实际 `scrollTop` 为 72.5px。数据集共 7 个入站，不扩大为数千条长列表性能验证。                                                                          |
| 选择与过滤              | 保留默认选中项，再选择 Vision；已选择筛选显示两项，未选择筛选排除这两项；搜索 mapping 显示两项匹配，同时总已选数量仍为 2。                                             |
| 取消和重开              | 不提交入站选择，关闭抽屉后重开，恢复原先 1 个已选入站和空搜索。                                                                                                        |
| 前端错误日志            | 本次检查采集的浏览器 error 日志为空；不代表所有页面均无错误。                                                                                                          |

表单测试完成后再次通过 API 深度比较：配置值、编排、完整配置和 Host 保持原样。只有订阅设置的 `updatedAt` 因正常保存而变化；恢复检查仅允许这个时间字段变化，没有忽略其他字段。

## 后续修复不混入上述版本证据

本次检查后的代码另修复了认证状态与 token 失配、内部组 refetch 覆盖选择草稿，以及 Vault reset/恢复非原子的问题。它们需要各自的 Actions 和新镜像验收，不能继承 `eb8f1790` 的浏览器结论。

Vault 新回归使用真实加密和 fake-indexeddb，验证事务提交、失败回滚和 blocked 升级行为，不等同于真实浏览器 IndexedDB、多标签页或实际磁盘失败。Passkey 流程回归不等同于硬件认证器验证。移动端、失败保存重试、完整长列表分页及多账号浏览器竞态仍需继续验证。

上述浏览器实测版本未包含 HeroUI。后续迁移代码见 [G0 代码检查点](heroui-g0-checkpoint.md)，尚未部署到此 VPS；完整范围见 [迁移清单](heroui-migration-scope.md)。
